import AIMapGenerator from '@/components/dm/AIMapGenerator';

export default function MapGeneratorPage() {
    return (
        <div className="min-h-screen bg-gradient-to-b from-gray-900 to-black p-8">
            <AIMapGenerator />
        </div>
    );
}
